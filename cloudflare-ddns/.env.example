# Example environment file for Cloudflare DDNS
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# ============================================
# Required Settings
# ============================================

# Your Cloudflare API Token
# Get it from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone.Zone (Read), Zone.DNS (Edit)
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here

# Domains to update (comma-separated, no spaces)
# Examples:
#   - Single domain: example.com
#   - Multiple domains: example.com,www.example.com
#   - Wildcard: *.example.com
#   - Mixed: example.com,www.example.com,*.example.com,subdomain.example.org
DOMAINS=example.com,www.example.com

# ============================================
# Optional Settings
# ============================================

# Enable Cloudflare proxy (orange cloud) - hides your real IP
# Set to "true" to enable, "false" to disable (DNS only mode)
PROXIED=false

# IPv6 Configuration
# Set to "none" to disable IPv6 completely
# IP6_PROVIDER=none

# Update schedule (cron format or @every syntax)
# Examples:
#   - @every 5m (every 5 minutes)
#   - @every 1h (every hour)
#   - 0 */6 * * * (every 6 hours)
# UPDATE_CRON=@every 5m

# DNS record TTL (Time To Live)
# 1 = automatic, or specify 120-86400 seconds
# TTL=1

# Comment for DNS records
# RECORD_COMMENT=Updated by Cloudflare DDNS on Windows

# ============================================
# Monitoring & Notifications (Optional)
# ============================================

# Healthchecks.io integration
# Get your URL from: https://healthchecks.io/
# HEALTHCHECKS=https://hc-ping.com/your-uuid-here

# Uptime Kuma integration
# Get push URL from your Uptime Kuma instance
# UPTIMEKUMA=http://your-uptime-kuma:3001/api/push/xxxxx

# Shoutrrr notifications (comma-separated URLs)
# Supports: Discord, Telegram, Slack, Email, Gotify, etc.
# Examples:
#   - Discord: discord://token@id
#   - Telegram: telegram://token@chatid
#   - Email: smtp://username:password@host:port/?from=from@example.com&to=to@example.com
# SHOUTRRR=
